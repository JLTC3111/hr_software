# Public client configuration only. Never use a service-role or secret key.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-or-anon-key
# Optional for web (defaults to the current origin); required for desktop builds.
# Password-reset emails complete on this existing HTTPS website.
VITE_APP_URL=https://hr.icue.vn
SMTP_HOST=mail90172.maychuemail.com
SMTP_PORT=587
SMTP_USER=dev@icue.vn
SMTP_PASS=
SMTP_FROM=dev@icue.vn
